/**
 * Type declarations for Node.js compatibility in Cloudflare Workers
 * These globals are available when using nodejs_compat_v2 compatibility flag
 */

declare const Buffer: typeof import("buffer").Buffer;
